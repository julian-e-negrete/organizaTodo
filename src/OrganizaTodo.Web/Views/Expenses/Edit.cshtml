@model OrganizaTodo.Web.Models.ViewModels.OtherExpenseViewModel
@{ ViewData["Title"] = "Editar Egreso"; }
<h4 class="mb-3">Editar Egreso</h4>
<div class="col-md-5">
    <form asp-action="Edit" method="post">
        @Html.AntiForgeryToken()
        <input asp-for="Id" type="hidden" />
        <partial name="_ExpenseForm" model="Model" />
        <div class="d-flex gap-2 mt-3">
            <button type="submit" class="btn btn-primary">Guardar</button>
            <a asp-action="Index" asp-route-month="@Model.Month" asp-route-year="@Model.Year" class="btn btn-outline-secondary">Cancelar</a>
        </div>
    </form>
</div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
